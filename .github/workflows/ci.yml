name: CI

on:
  push:
    branches:
      - master
  pull_request:
    paths:
      - "src/**"
      - "tests/**"
      - "uv.lock"
      - "pyproject.toml"
      - ".github/workflows/**"

jobs:
  prek:
    name: Prek
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: j178/prek-action@v2

  typos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: crate-ci/typos@master

  basedpyright:
    name: BasedPyright
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          python-version: 3.12
          version: "latest"

      - name: Install Dependencies
        run: uv sync --locked

      - name: Static typing check
        run: uvx basedpyright

  test:
    name: Coverage
    # runs-on: ${{ matrix.os }}
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    concurrency:
      # group: test-coverage-${{ github.ref }}-${{ matrix.python-version }}-${{ matrix.os }}
      group: coverage-${{ github.ref }}-${{ matrix.python-version }}
      cancel-in-progress: true

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
        # os: [ubuntu-latest, windows-latest, macos-latest]
        # pydantic-version: ["v1", "v2"]

    env:
      # OS: ${{ matrix.os }}
      PYTHON: ${{ matrix.python-version }}
      UV_NO_SYNC: 1

    steps:
      - uses: actions/checkout@v6

      - name: Setup uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}
          version: latest

      - name: Install Dependencies
        run: uv sync --locked

      - name: Run Pytest
        run: uv run poe test

      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@v5
        with:
          env_vars: PYTHON
          use_oidc: true
          report_type: test_results

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          env_vars: PYTHON
          use_oidc: true

  e2e:
    name: E2E (real le3-api)
    runs-on: ubuntu-latest
    # 测试本身无 secrets 时会自动 skip，无需 job 级判断
    steps:
      - uses: actions/checkout@v6

      - name: Setup uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          python-version: 3.12
          version: "latest"

      - name: Install Dependencies
        run: uv sync --locked

      - name: Install Chromium for rendering
        run: uv run python -m playwright install chromium

      - name: Run E2E tests
        run: uv run pytest tests/e2e_test.py -v
        env:
          AZURLANE_TEST_UID: ${{ secrets.AZURLANE_TEST_UID }}
          AZURLANE_TEST_SERVER_ID: ${{ secrets.AZURLANE_TEST_SERVER_ID }}
          # 本地 uv run 会读 .env 注入 RENDER__PROVIDER；CI 无 .env，需显式指定
          RENDER__PROVIDER: playwright
